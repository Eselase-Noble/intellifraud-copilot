services:
  api:
    build: .
    env_file:
      - .env
    ports:
      - "8000:8000"
    command: >
      sh -c "python -m app.db.seed_data &&
             uvicorn app.main:app --host 0.0.0.0 --port 8000"
